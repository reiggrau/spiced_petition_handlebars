<div class="sectionDiv">
    <h1>You are now a House Representative!</h1>
    <h4>You can check your profile information below.</h4>
</div>
<div class="sectionDiv" id="stepsDiv">
    <h2>Edit profile</h2>
    <p>Change anything you want from your profile and let the PR team do the rest.</p>
    <form action="/profile" method="POST">
        <h4>Name:</h4>
        <p>{{#if error_first_name}}<font color="red"> You must write a first name!</font>{{/if}}</p>
        <input name="first_name" placeholder=" First Name" value="{{first_name}}" />
        <p>{{#if error_last_name}}<font color="red"> You must write a last name!</font>{{/if}}</p>
        <input name="last_name" placeholder=" Last Name" value="{{last_name}}" />
        <h4>email and password:</h4>
        <p>{{#if error_email}}<font color="red"> You must write an email!</font>{{else if error_email_used}}<font color="red"> Email already in use!</font>{{/if}}</p>
        <input name="email" placeholder=" example@email.com" value="{{email}}" />
        <p>{{#if error_password}}<font color="red"> You must write a password!</font>{{/if}}</p>
        <input type="password" name="password" placeholder=" password" />
        <h4>Profile picture (optional):</h4>
        <input type="url" class="longTxt" name="image_url" placeholder=" Image URL" value="{{image_url}}" />
        <h4>Campaign quote (optional):</h4>
        <input type="text" class="longTxt" name="quote" placeholder=" Quote" value="{{quote}}" />
        <h4>Political party (optional):</h4>
        <input list="party" name="party" placeholder=" Party" value="{{party}}">
        <datalist id="party">
            <option value="Conservative">
            <option value="Liberal">
            <option value="Capitalist">
            <option value="Socialist">
            <option value="Environmentalist">
        </datalist>
        <h4>User page (optional):</h4>
        <input type="url" class="longTxt" name="user_page" placeholder=" User page" value="{{user_page}}" />
        <div>
            <p><input type="submit" value=" Save changes " /></p>
        </div>
    </form>
    <form action="/deleteuser" method="POST">
        <input id="deleteButton" class="log" type="submit" value=" delete user ❌ {{id}}" />
        <input type="hidden" name="id" value="{{id}}" />
    </form>
</div>