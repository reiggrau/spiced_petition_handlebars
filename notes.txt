

1. server.js
- routes
- set cookie after signature

2. Handlebar templates





// DATA LAYER
1. Database (should have table):
Signatures:
- first_name (string)
- last_name (string)
- signature ()

2. db.js
Functions:
- createSignature (to store signature to database)
- getAllSignatures (to show on signatures page)
- 


// STEPS
0. Create basic server
1. Create main page handelbars
2. Create thank you handelbars
3. Make css

-----
- Register page
- Hash the password (hashPassword)
- Once the user has registered (insertUser) > redirect to petition (remember to hash password)
- (login) before checking password, check if email exists (findUserByEmail) - return user_id, email, password "SELECT user_id, email, password FROM users WHERE email = $1"
- Authenticate (email, password) - findUserByEmail > compare passwords
