<div id="thankyouDiv" class="sectionDiv">
    <h1>THANK YOU FOR YOUR PETITION</h1>
    <h3>Your petition has been successfully submitted and will be reviewed soon.</h3>
    {{!-- petition template --}}
    {{#petitionData}}
    <div class="petitionDiv">
        <div class="petitionUserDiv">
            <a href="{{user_page}}"><img id="userImg" src={{#if image_url}}{{image_url}}{{else}}"./assets/default_user.jpeg"{{/if}} alt="signature" /></a>
            <h4 id="userTxt">{{first_name}} {{last_name}}</h4>
        </div>
        <div class="petitionContent">
            {{#if canDelete}}
                <form action="/deletepetition" method="POST">
                    <input id="deleteButton" class="log" type="submit" value=" delete ❌ " />
                    <input type="hidden" name="petition_id" value="{{petition_id}}" />
                </form>
            {{/if}}
            <h3>{{title}}</h3>
            <p>{{petition}}</p>
        </div>
        <div id="petitionDate">
        <p>{{created_at}}</p>
        <img id="signatureImg" src="{{signature_url}}" alt="signature">
        </div>
    </div>
    {{/petitionData}}
    <h4>So far, a total of <span style="font-size:25px"> {{petition_count}} </span> petitions have been submitted by <span style="font-size:25px"> {{representatives_count}} </span> House Representatives.</h4>
</div>
<div class="sectionDiv">
    <h3>Check other petitions:</h3>
        <div class="miniDivContainer">
        {{#miniPetitions}}
            <div class="miniDiv">
                <a href="{{user_page}}"><img id="miniImg" src={{#if image_url}}{{image_url}}{{else}}"./assets/default_user.jpeg"{{/if}} alt=""></a>
                <div class="miniContent">
                    <h3>{{title}}</h3>
                    <p>{{topic}}</p>
                </div>
            </div>
        {{/miniPetitions}}
        </div>
</div>